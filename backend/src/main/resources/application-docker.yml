# Docker / docker-compose: DB는 동일 네트워크의 mysql 서비스 이름으로 접근한다.
# 로컬에서 ./mvnw 실행 시에는 기본 application.yml(localhost)을 쓴다.
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/inspection_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: ${DB_PASSWORD:your_password}
